package com.singleton;

import com.designpatterns.DesignPatternsAssignment.ConfigurationManager;

public class MainClass {
	
	

	public static void main(String[] args) {
		//Design a Logger class using Singleton pattern. Ensure only one logger object is used throughout the application. Add a method log(String msg).
//		Logger log1=Logger.loggerCreate();
//		log1.log("Hello..!");
//		System.out.println(log1.hashCode());
//		Logger log2=Logger.loggerCreate();
//		System.out.println(log2.hashCode());
//		log2.log("world..!");
		
		//Create a ConfigurationManager using Singleton that loads app settings only once. Demonstrate that multiple calls return same instance.
//		ConfigurationManager cm1=ConfigurationManager.getInstane();
//		cm1.appSettings();
//		ConfigurationManager cm2=ConfigurationManager.getInstane();
//		cm2.appSettings();
		
		//- Implement a DatabaseConnection class using Singleton. Prevent cloning and reflection from breaking the Singleton.
		
	}

}
